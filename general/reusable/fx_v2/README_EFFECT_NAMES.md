# Calling promoted effects

Use the neutral names in `PROMOTED_EFFECT_NAMES.txt` and call `apply_effect` from `promoted_effects.py`. Do not call effects by a song, film, character or prior-project identifier. Historical labels resolve through `effect_aliases.json` only for compatibility.
