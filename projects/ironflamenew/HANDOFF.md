# IronFlameNew Handoff

Authority: current user instruction -> this branch guarded state/manifests -> current main. Historical chats/projects remain denied.

Current stage: `SHOT_PROOFS_ACCEPTED`.

All three supplied 6.04 s references were fully extracted (145/145 each). Final media does not pad or paste the portrait clips; it uses source-derived silhouette/contour/contact/faceted/ribbon geometry and all-frame motion drivers in newly authored 1280x720 compositions.

No intermediate compiled video is authorized. Run the canonical FX v2 gate, verify `fx.lock.json`, then render only the final movie from the byte-identified external inputs in `FINAL_RENDER_INPUT_HASHES.json`.
