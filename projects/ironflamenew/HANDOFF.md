# IronFlameNew Handoff

## Authority
Current user instruction -> this branch's guarded state/manifests -> current main. No historical production context is authorized.

## Current stage
`SOURCE_INGESTED`.

## Required next action
Extract all 145 frames from each of the three short supplied references (435 frames total), preserve frame identity/ranges, analyze visual/motion language, update the reference manifest and only then attempt `REFERENCES_ANALYZED`.

Do not generate storyboards, visual DNA, characters, stills or production imagery before that gate passes.
