# System authority

This project consumes the current `main` AIVideoEdit operating system through `bootstrap.py`. Reusable effects and non-effect production workflows are selected from the current-main neutral registries according to each scene's production mode and media capabilities.
