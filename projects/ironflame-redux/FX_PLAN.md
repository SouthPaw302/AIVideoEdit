# IronFlameRedux FX Plan

Status: not locked.

FX will be selected only after source intake and shot design. Reuse the canonical FX V2 registry first; production use requires an exact `.fx.json` manifest, declared render inputs, a passing fail-closed precompile gate, and lock verification immediately before render.
