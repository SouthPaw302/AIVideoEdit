# IronFlameRedux Status

Stage: intake / awaiting source files.

Completed:
- production branch created from `main`;
- repository doctrine, pipeline, style contract, reusable FX policy, FX V2 registry, and precompile gate reviewed;
- project scaffold initialized.

Blocked only on user-supplied production files. Do not begin storyboard, shot generation, or render work until those files are inspected.
