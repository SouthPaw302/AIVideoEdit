# IronFlameRedux Status

Stage: **reference intake complete / visual DNA + storyboard production map locked / audio intake pending.**

## Completed
- correct production branch confirmed: `song/ironflame-redux`;
- repository doctrine, production pipeline, style contract, FX V2 registry and fail-closed gate reviewed;
- current-production storyboard received, inspected and hashed;
- storyboard identity/world/palette/motion language distilled into `VISUAL_DNA.md`;
- sequence board expanded into a multi-shot production map in `SHOT_LIST.md` so the film cannot collapse into long static holds;
- approved-vs-blocked canonical FX families mapped in `FX_PLAN.md`;
- asset provenance recorded in `ASSET_MANIFEST.json` and `SOURCE_INTAKE.md`.

## Current constraints
The Redux audio master has not yet been received into this production. Therefore:
- storyboard timecodes are reference only;
- no Redux audio control bus may be finalized;
- no song-synced timing/transition lock may be claimed;
- no final FX requirements manifest or full-film compile should be created yet.

## Next production move
On audio receipt: inspect/measure master -> confirm lyrics/story timing -> derive preserved 24 fps control bus -> reconcile storyboard beat ranges to actual runtime -> create first four representative source/identity frames and short living-shot proofs -> human/visual QC -> scale accepted shot packages only.
