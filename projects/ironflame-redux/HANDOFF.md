# IronFlameRedux Handoff

Branch: `song/ironflame-redux`

Current phase: reference intake complete; visual direction/storyboard production map established; Redux audio master still pending.

## Current-production authority
The user supplied `Ironflame: A Mythic Visual Storyboard.png` for this production. Its provenance/hash is recorded in `SOURCE_INTAKE.md` and `ASSET_MANIFEST.json`. Treat it as the current visual/story authority. Do not import historical IronFlame branch story/media/prompts/controls/renders unless the user explicitly requests them for Redux.

## Locked direction
Read, in order:
1. `SOURCE_INTAKE.md`
2. `VISUAL_DNA.md`
3. `SHOT_LIST.md`
4. `FX_PLAN.md`
5. root `AGENT_HANDOFF.md`, `BIBLE.md`, `SYSTEM_INDEX.md`, current production pipeline/style contract, and FX V2 registry/gate.

The protagonist is a single recurring female IronFlame. Preserve her identity. The film is dark-folk mythic naturalism with forge/fire/iron/oath/wolves/journey/legacy motifs and an ember-orange -> iron-blue -> ash-gray -> dawn-gold progression.

## Timeline warning
The board says 3:20 but also contains a 03:20-03:28 epilogue and 03:28+ end card. The actual audio master decides runtime. Never extend/compress the music to fit infographic labels.

## Next actions after audio arrives
1. hash/probe the master and preserve exact specs;
2. obtain/confirm lyrics or transcript and reconcile board beats to actual musical structure;
3. derive one frame-aligned 24 fps reactive control bus from the Redux master;
4. create/lock a clean protagonist identity anchor plus first representative forge, cool-exterior, and dawn/ruin source frames;
5. build four short real shot proofs using only approved canonical FX or truthfully documented non-FX animation methods;
6. run identity/artifact/temporal QC and KEEP/REVISE/REJECT each proof;
7. scale only the accepted visual grammar into full shot packages;
8. create final exact FX requirements only after the shot set is known; run fail-closed gate and verify the lock immediately before production renders;
9. assemble only after enough finished shots exist; inspect the actual final export.

Never treat gate success, image generation, or a storyboard frame as a finished shot.
