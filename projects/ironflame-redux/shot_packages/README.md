# Shot Packages

Create one directory per selected shot using the canonical production layout: `source/`, `alpha/`, `layers/`, `depth/`, `generated/`, `fx_assets/`, `transition/`, `loop/`, `preview/`, and `notes/` as required.
