# Silver Coin Test 2 — Render Script

The single uploaded square source clip is treated as the complete visual source. Eight new chapters use different bounded crops, mirrors, color treatments, and editorial transitions. No prior Silver Coin media enters the render.

The render implementation is `render_test2.py`.
