# AIVideoEdit
**First production action in every new Copilot/agent session:** run `python bootstrap.py boot --repo-root <repo>`.
Do not bypass the bootstrapped current-main OS with copied/stale instructions. After PASS, use `SOUL.md`, `.aivideoedit/SECOND_BRAIN.md`, and the bootstrapped production guard. Tool/vendor-specific behavior never overrides the AIVideoEdit OS.
